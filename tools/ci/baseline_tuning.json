{
  "swept": "nim r --path:src tools/tune_baselines.nim",
  "note": "The head-to-head sweep's pick, over a 54-cell grid and four map seeds per cell. It RETUNED the design note's proposal (2, 8, 6, 2 -> 623 vs 508 for sapper against camper), which the note anticipates as an errata rather than a redesign. tests/test_pom_tuning.nim asserts the shipped DefaultBaselineParams still equal it, so a hand-edit of the defaults fails the build; ci.yml re-runs the tool with --check at the horizon it was tuned at.",
  "bombEnemyRange": 3,
  "powerupSearch": 4,
  "dodgeHorizon": 8,
  "campExits": 2
}
